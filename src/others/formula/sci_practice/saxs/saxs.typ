== ABSTRACT

== INTRODUCTION

== BACKGROUND THEORY

=== Scaling Theory

$P(q)$ - the form factor for a single chain

$S(q)$ - the structure factor for a single chain

$I(q)$ - the inter-chain interactions

Relation between $P(q)$, $S(q)$ and $I(q)$:

$
  I(q) ~ P(q) S(q)
$

The form factor $P(q)$ is defined as:

$
  P(q, L, b) = cases(
    P_1(q, L, b)\, quad q b < 3.1,
    P_2(q, L, b)\, quad q b >= 3.1
  )
$

with:

$
  P_1(q, L, b) = (1 - w(q R_G)) 2 / u_1^2 ()
$

$
  I(q) = k P(q, L, b) / (1 + beta c(q, xi) P(q, L, b))
$


== EXPERIMENTS

== RESULTS

$
  I_"sub" = I_"exp" - I_"bkg"
$
